[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mpai-agent-sdk"
version = "0.1.0"
description = "MPAI agent integration SDK"
requires-python = ">=3.8"
